# Flask backend base URL used by prediction, normal chat, Agentic RAG, and static example APIs.
# Render: set this to https://<your-backend-service>.onrender.com before npm run build.
VITE_API_BASE_URL=http://localhost:5000
